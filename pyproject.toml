[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jobsdb-assistant"
dynamic = ["version"]
description = "Local, human-approved AI job search and application assistant for JobsDB Hong Kong"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "User"}
]
keywords = ["jobsdb", "automation", "resume", "job-application"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "playwright>=1.40.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=4.9.0",
    "numpy>=1.24.0",
    "loguru>=0.7.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "typer>=0.9.0",
    "python-dotenv>=1.0.0",
    "PyYAML>=6.0,<7",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=7.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]

[project.scripts]
jobsdb-assistant = "src.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "config*", "tests*"]

[tool.setuptools.dynamic]
version = {attr = "src.version.__version__"}

[tool.black]
line-length = 100
target-version = ['py39']

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
# v2.0: 测试三分类 marker
markers = [
    "e2e: 需要真实 JobsDB 网络 + 手动登录(慢,CI 不跑,默认跳过)",
    "characterization: 锁定 v1.0 行为的安全网(快,CI 跑)",
    "unit: 纯单元测试,不依赖浏览器(快,CI 必跑)",
]
# 默认不跑 e2e(本地 pytest 也不会误起浏览器)
addopts = "-m 'not e2e'"

[tool.coverage.run]
branch = true
source = ["src"]
# main.py 是 CLI 入口(@app.command 处理器 + rich console I/O),
# 经手动/CLI 测试,不纳入单测覆盖率统计(其逻辑由被调模块覆盖)
omit = ["*/tests/*", "*/__pycache__/*", "*/main.py"]

[tool.coverage.report]
# v0.1.0 public release gate: combined line and branch coverage >= 80%
fail_under = 80
show_missing = true
