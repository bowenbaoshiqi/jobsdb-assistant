interface:
  display_name: "JobsDB Assistant"
  short_description: "Run the complete local JobsDB workflow"
  default_prompt: "Use $jobsdb-assistant to start my local job search and keep processing Dashboard work until I stop it."
